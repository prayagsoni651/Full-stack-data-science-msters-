# 8. Why is eggs a valid variable name while 100 is invalid?


"""

1. Vlid Variable name Must Start With A latter (a-z,A-Z)
   or an underscore (_). They Can not Start With A number
   
   > eggs Start with the latter "e", so it statific 
     this rule and is a valid variable name 
     
2. After The first charecter , variable name can
contain latters (a-z,A-Z), NUmbers(0-9) and underscore
(_) but no other special character 

  > 'eggs' contains only latters, so it satifies this 
  rule and is a valid variable name.
  
  
  
  
"""